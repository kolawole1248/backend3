<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head', {title}) %>
    <style>
        .management-container {
            max-width: 600px;
            margin: 2rem auto;
            padding: 2rem;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        
        .welcome-message {
            background: #d4edda;
            color: #155724;
            padding: 1rem;
            border-radius: 4px;
            margin-bottom: 1.5rem;
            border-left: 4px solid #28a745;
        }
        
        .management-links {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-top: 2rem;
        }
        
        .management-link {
            display: block;
            padding: 1rem;
            background: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            text-align: center;
            transition: background 0.3s;
        }
        
        .management-link:hover {
            background: #0056b3;
        }
    </style>
</head>
<body>
    <header>
        <%- include('../partials/header') %>
    </header>
    <nav>
        <%- include('../partials/navigation') %>
    </nav>
    <main>
        <div class="management-container">
            <h1><%= title %></h1>
            
            <!-- Flash Messages -->
            <%- messages() %>
            
            <div class="welcome-message">
                <strong>Success!</strong> You're logged in.
            </div>
            
            <p>Welcome to your account management dashboard. Here you can manage your account settings and preferences.</p>
            
            <div class="management-links">
                <a href="/account/update" class="management-link">
                    Update Account Information
                </a>
                <a href="/inv/" class="management-link">
                    Inventory Management
                </a>
                <a href="/account/logout" class="management-link" style="background: #dc3545;">
                    Logout
                </a>
            </div>
        </div>
    </main>
    <footer>
        <%- include('../partials/footer') %>
    </footer>
</body>
</html>