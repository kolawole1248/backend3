<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head', {title}) %>
    <style>
        .error-container {
            text-align: center;
            padding: 50px 20px;
            max-width: 800px;
            margin: 0 auto;
        }
        .error-code {
            font-size: 6rem;
            font-weight: bold;
            color: #ff6b6b;
            margin-bottom: 20px;
        }
        .error-message {
            font-size: 1.5rem;
            margin-bottom: 30px;
            color: #333;
        }
        .error-image {
            max-width: 300px;
            margin: 30px auto;
        }
        .home-link {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .home-link:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <header>
        <%- include('../partials/header') %>
    </header>
    <nav>
        <%- include('../partials/navigation') %>
    </nav>
    <main>
        <div class="error-container">
            <div class="error-code"><%= title %></div>
            <div class="error-message"><%= message %></div>
            
            <!-- Fun 404 image for page not found errors -->
            <% if (title == 404) { %>
                <img class="error-image" src="/images/errors/404-error.jpg" alt="Page not found" />
                <p>Don't worry, even the best mechanics lose a tool sometimes!</p>
            <% } else { %>
                <img class="error-image" src="/images/errors/server-error.jpg" alt="Server error" />
                <p>Our mechanics are working hard to fix this!</p>
            <% } %>
            
            <a href="/" class="home-link">Take Me Home</a>
        </div>
    </main>
    <footer>
        <%- include('../partials/footer') %>
    </footer>
</body>
</html>